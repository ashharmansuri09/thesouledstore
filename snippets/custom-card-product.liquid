<a href="{{ product.url }}" class="custom-product-card">
    <div class="custom-product-card__image">
      {% if product.featured_image %}
        <img
          src="{{ product.featured_image | image_url: width: 600 }}"
          alt="{{ product.title | escape }}"
          loading="lazy"
          width=""
          height=""
        >
      {% endif %}
    </div>
    <div class="product-card__info">
      {% assign color_option = '' %}
      {% for option in product.options_with_values %}
        {% if option.name == 'Color' and option.values.size > 0 %}
          {% assign color_option = option.values[0] %}
        {% endif %}
      {% endfor %}
      <h3 class="product-card__title">
        {{ product.title -}}
        {%- if color_option != '' %} : {{ color_option }}{% endif %}
      </h3>
     <h4 class="product-cart__category">{{ product.collections.first.title }}</h4>
      <span class="product-card__price">
        {{ product.price | money_with_currency }} 
      </span>
    </div>
  </a>